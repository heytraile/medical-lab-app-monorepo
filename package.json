{
  "name": "drax-hall-lis",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "postinstall": "pnpm --filter @drax-lis/contracts build && pnpm --filter @drax-lis/protocols build",
    "dev:edge": "turbo run dev --filter=@drax-lis/edge-engine --filter=@drax-lis/web",
    "dev:cloud": "turbo run dev --filter=@drax-lis/api --filter=@drax-lis/web",
    "dev:sim": "turbo run dev --filter=@drax-lis/simulators",
    "db:generate": "turbo run db:generate --filter=@drax-lis/edge-engine",
    "db:push": "turbo run db:push --filter=@drax-lis/edge-engine"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  }
}
